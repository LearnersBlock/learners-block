version: '3.4'

services:
    controller:
        build:
            context: ./controller
            dockerfile: Dockerfile.dev.controller
        ports: 
            - "9090:9090"
        labels:
            portainer: hidden
        volumes:
            - './controller/apps/backend:/app'

    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile.dev
        ports: 
            - "8081:8081"
        labels:
            portainer: hidden
        volumes:
            - './frontend/apps/interface/dist/spa:/app/web/public/interface'
            - './frontend/apps/interface:/app/web/public/interface_dev:cached'
            - './frontend/apps/library/dist/spa:/app/web/public/library:cached'
            - './frontend/apps/file-manager/dist:/app/web/public/files'
            - 'yarn_cache_frontend:/usr/local/share/.cache/yarn/'

volumes:
    yarn_cache_frontend:
