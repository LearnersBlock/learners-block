version: '2.1'

services:
    build:
        build:
            context: .
            dockerfile: Dockerfile.build
        volumes:
            - ./frontend/apps/file-manager/dist:/export/filemanager
            - ./frontend/apps/interface/dist/spa:/export/interface
            - ./controller/apps/wifi-connect/ui/build:/export/controller
